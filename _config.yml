title: Raffaele P. — ML Projects
description: Notes from learning machine learning · experiments, write-ups & papers
markdown: kramdown
kramdown:
  syntax_highlighter: rouge

# Keep build tooling and design docs out of the generated site. docs/ holds
# specs/plans whose fenced Liquid examples would otherwise break the build.
exclude:
  - docs/
  - vendor/
  - Gemfile
  - Gemfile.lock
  - README.md
  - .superpowers/

collections:
  projects:
    output: true
    permalink: /projects/:name/
  writing:
    output: true
    permalink: /writing/:name/
  papers:
    output: true
    permalink: /papers/:name/
  books:
    output: true
    permalink: /books/:name/
  talks:
    output: true
    permalink: /talks/:name/
